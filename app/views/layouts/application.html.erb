<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= yield(:title) %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css?family=Roboto:400,700' %>
    <%= stylesheet_link_tag 'application', 'https://fonts.googleapis.com/icon?family=Material+Icons' %>
    <%= stylesheet_link_tag 'application', 'https://code.getmdl.io/1.1.3/material.indigo-pink.min.css' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'https://code.getmdl.io/1.1.3/material.min.js' %>
  </head>

  <body>
    <% title "Your custom title" %>
    <div class="mdl-layout__container">
      <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
        <header class="mdl-layout__header">
            <div class="mdl-layout__header-row">
                <div class="logo"><%= image_tag('FRHC_Logo.jpg') %></div>
                <span class="mdl-layout-title">Farm & Ranch Insurance Services, Inc.</span>
                <div class="mdl-layout-spacer"></div>
                <nav class="mdl-navigation" style="margin-left: 120px">
                    <%= link_to "Home", home_index_path, :class => "mdl-navigation__link" %>
                    <%= link_to "Who We Are", home_about_path, :class => "mdl-navigation__link" %>
                    <%= link_to "Join Our Team", home_agents_path, :class => "mdl-navigation__link" %>
                    <%= link_to "Our Products", home_products_path, :class => "mdl-navigation__link" %>
                    <%= link_to "Lets Talk", home_contact_path, :class => "mdl-navigation__link" %>
                    <a id="login-button" class="mdl-navigation__link">Agent Login
                    </a>
                    <div class="animated-dialog-content">
                        <div class="animated-dialog-close">&times;</div>
                        <div id="firebase-auth-container">
                          <div class="mdl-card__supporting-text mdl-color-text--grey-600">
                            <% if current_user && current_user.admin? %>
                              Signed in as: <strong><%= current_user.name %></strong>
                              <%= link_to('Control Panel', admin_index_path, class: "mdl-button mdl-js-button mdl-button--raised") %>
                              <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "mdl-button mdl-js-button mdl-button--raised") %>
                            <% elsif current_user %>
                              Signed in as: <strong><%= current_user.name %></strong>
                              <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "mdl-button mdl-js-button mdl-button--raised") %>
                            <% else %>
                            <%= render partial: "devise/sessions/sign_in" %>
                            <% end %>

                          </div>
                    </div>
                  </div>
                </nav>
            </div>
        </header>
    <%= yield %>
  </body>
</html>
